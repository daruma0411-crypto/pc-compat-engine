# PC互換性判定エンジン + エージェント群 Dependencies
# =====================================================

# ── Web Framework (Render デプロイ用) ─────────────────
flask>=3.0.0
gunicorn>=22.0.0

# ── LLM Providers ────────────────────────────────────
anthropic>=0.40.0
openai>=1.50.0

# ── Data / Storage ───────────────────────────────────
google-cloud-bigquery>=3.25.0
chromadb>=0.5.0

# ── HTTP Client ──────────────────────────────────────
# PageIndex API 呼び出し
httpx>=0.27.0
# ManualCollectorAgent: PDF非同期ダウンロード
aiohttp>=3.10.0

# ── X (Twitter) API ──────────────────────────────────
# XMonitorAgent / XReplyAgent: FilteredStream + リプライ送信
tweepy>=4.14.0

# ── Browser Automation ───────────────────────────────
# PCPartsScraperAgent: JS描画ページのスペック収集
# インストール後に `playwright install chromium` も必要
playwright>=1.47.0

# ── PDF Parser ───────────────────────────────────────
# ManualCollectorAgent: TOC検出・テキスト抽出
pdfplumber>=0.11.0

# ── Utilities ────────────────────────────────────────
python-dotenv>=1.0.0
