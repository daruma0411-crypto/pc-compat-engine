services:
  - type: web
    name: pc-compat-engine
    env: python
    buildCommand: pip install -r requirements_render.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # Renderダッシュボードで手動設定してください
